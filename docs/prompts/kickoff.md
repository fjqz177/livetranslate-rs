# 开工卡——任何工作包动第一行代码前过一遍

出口条件（全部满足才进施工）：

1. [ ] **路由**：对照 AGENTS.md §4 路由表，把本包触发的必读文档读完（回执：读过的清单）。
2. [ ] **摸底**：codegraph_explore 相关符号与调用链；列出预计改动文件清单（不许可「顺手多改」）。
3. [ ] **立档判据**（docs/README.md 纪律②）：有要落 D-xx/ADR-x 的裁决、或多步施工要验收清单
       ——满足其一 → 立档（docs/drafts/ 起草，六节骨架：背景取证→方案裁决→施工卡→验收基线→遗留走查）；
       皆无 → 小修不立档不给号，commit message 说清。
4. [ ] **涉用户裁决**：先出「裁决请求卡」等逐条拍板；AGENTS §8 待拍板块命中项，未点头禁动工。
5. [ ] **涉契约**（lt-proto / Settings）：核对 AGENTS §3 冻结规则。属评审类（删除/改名/改型/改语义）
       → 先登记 D-xx 再动；豁免类（纯新增变体/字段）也要 serde default 兼容旧档，并判断
       PROTO_VERSION 是否须递增。
6. [ ] **顺序**：定稿 docs(scope) 提交必须早于第一行实现代码。

子代理分工（AGENTS §5）：检索/执行 → flash 级 general-purpose/Explore；
承重墙（Win32、链接 CRT、下载器、算法移植）→ 主线程亲自做。
