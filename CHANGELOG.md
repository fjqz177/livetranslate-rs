# 更新日志

## [1.0.0] - 2026-09-19

首个正式版本。

- **实时音频翻译**：抓取电脑正在播放的声音（可叠加麦克风），本地语音识别 + AI 翻译，悬浮窗 / OBS 字幕窗 / 控制面板同步显示原文与译文；看视频、开会、直播字幕都适用。
- **三种识别引擎，纯本地运行**：SenseVoice（默认）、Whisper（tiny / base / small / medium / large-v3 / turbo 六档）、Qwen3，全部纯 CPU，模型下载后离线可用。
- **模型下载省心**：断点续传 + 逐文件 sha256 完整性校验，下载源 HuggingFace / ModelScope 可切换；每次加载再验一遍 sha256，发现坏文件自动隔离并重新下载，无需手动处理。
- **翻译供应商开箱即用**：内置 DeepSeek、OpenAI、GLM（智谱）、Kimi、Qwen（通义）、Doubao（火山方舟）云端预设和 LM Studio、Ollama 本地预设——选预设、填 key 即用；悬浮窗下拉一键热切换，正在进行的翻译平稳收尾。
- **测试连接不卡界面**：独立线程即点即测，结果四态、带具体原因，不再永久等待。
- **翻译结果可靠**：思维链不会混进译文；请求失败自动逐级降级重试；报错与译文一眼可辨；可携带上文语境（轮数可调）；翻译随时可取消。
- **费用透明**：按本次运行累计、重启清零，不同接口按各自币种分别记账。
- **多窗口 + 托盘常驻**：可拖动置顶悬浮窗、OBS 可捕获的独立字幕窗、中英双语控制面板、带未读计数与贴底跟随的日志窗；托盘图标随时显隐 / 暂停恢复 / 退出。
- **退出干净**：统一确认窗（Enter 确认 / ESC 取消）；退出时正在识别的半句话照常完成识别与翻译再收尾，不丢尾巴。
- **数据集中好迁移**：配置、模型、转写记录、日志全在一个目录（`~/.config/livetranslate`），换新 exe 即完成升级；转写记录自动落盘。
- **单实例**：重复启动自动唤起已有窗口，不会开出第二个。
